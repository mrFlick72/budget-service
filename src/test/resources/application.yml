attachment:
  repository:
    access-key:
    secret-key:
    bucket-name: family-budget-data/dev
    bucket-prefix: /dev
    region: eu-central-1

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost